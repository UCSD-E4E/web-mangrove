setup:
  config: {}
build:
  packages:
    - gdal-bin
  languages:
    - >-
      https://buildpack-registry.s3.amazonaws.com/buildpacks/heroku-community/apt.tgz
    - python
run:
  web: 'gunicorn app:app'
