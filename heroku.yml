setup:
  config:
    BUILDPACK_URL: 'https://github.com/heroku/heroku-geo-buildpack.git'
build:
  packages:
    - gdal-bin
  languages:
    - 'https://github.com/heroku/heroku-geo-buildpack.git'
    - python
run:
  web: 'gunicorn app:app'

