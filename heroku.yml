setup:
  config:
    BUILDPACK_URL: 'https://github.com/mojodna/heroku-buildpack-gdal.git'
build:
  packages:
    - gdal-bin
  languages:
    - >-
      https://buildpack-registry.s3.amazonaws.com/buildpacks/heroku-community/apt.tgz
    - python
  
